# Copy to `web/.env` for local Vite dev (`npm run dev` loads this directory).
# API calls default to same-origin `/api/v1` via the Vite proxy — leave empty to use that.

# Optional: override API base (e.g. https://dashboard.example.com/api/v1 for a remote BFF)
VITE_API_BASE=/api/v1
