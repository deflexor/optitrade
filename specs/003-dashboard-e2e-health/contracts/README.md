# HTTP contracts (dashboard BFF)

JSON over `GET`/`POST` under `/api/v1` (cookie session on protected routes). See:

- [error-response.md](./error-response.md) — failure envelope
- [health-get.md](./health-get.md) — public health
- [overview-fragments.md](./overview-fragments.md) — overview slices touched by this feature

Breaking field renames or semantic changes require coordinated UI updates per constitution **II**.
